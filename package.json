{
  "name": "beshmarai-free",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm ci --prefix apps/site && npm ci --prefix apps/web",
    "typecheck": "npm run typecheck --prefix apps/site && npm run typecheck --prefix apps/web",
    "verify:public": "node scripts/verify-public-boundaries.mjs",
    "verify:models": "node scripts/verify-model-package.mjs",
    "build:site": "npm run build --prefix apps/site",
    "build:web": "npm run build --prefix apps/web",
    "assemble": "node scripts/build-pages.mjs",
    "build": "npm run verify:public && npm run verify:models && npm run build:site && npm run build:web && npm run assemble"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
